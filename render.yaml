services:
  - type: web
    name: stockflow-pos
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: node server.js
    publishDir: dist
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: stockflow-db
          property: connectionString

databases:
  - name: stockflow-db
    databaseName: stock_gxpq
    user: moselyzm
